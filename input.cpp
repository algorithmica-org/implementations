#include <bits/stdc++.h>
using namespace std;

int main(){
	clock_t start = clock();
	for(int i = 0; i < 1e6; i++){
		int t;
		scanf("%d", &t);
	}
	cout << double(clock()-start) / CLOCKS_PER_SEC;

	return 0;
}
